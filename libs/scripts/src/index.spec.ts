// import * as scripts from './index';
import chalk from 'chalk';
describe('scripts:index', function () {
  describe('basic', function () {
    it('should exists', () => {
      expect(true).toBeDefined();
    });
  });
});
