import Repository from './repository';

describe("Repository", () => {
  describe("Repository:get()", function () {

   test('should return the data in promise', async () => {
      expect(Repository).toBeDefined();
    });
  });
});