import { GLOBAL_OPTIONS } from '../../../globals';
import { withEditTabs } from './Common';
export default withEditTabs({
  pageData: {
    items: [
      
    ],
  },
  general: [
    {
      name: 'sitekey',
      cmpType: 'Input',
      label: 'sitekey',
    },
  ],
});
