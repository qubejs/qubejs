{
  "name": "localserver",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/localserver/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/webpack:webpack",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "target": "node",
        "compiler": "tsc",
        "outputPath": "dist/apps/localserver",
        "main": "apps/localserver/src/main.ts",
        "tsConfig": "apps/localserver/tsconfig.app.json",
        "assets": ["apps/localserver/src/assets"],
        "webpackConfig": "apps/localserver/webpack.config.js"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "localserver:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "localserver:build:development"
        },
        "production": {
          "buildTarget": "localserver:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/localserver/jest.config.ts"
      }
    }
  }
}
